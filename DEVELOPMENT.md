# KOAN Plus の目的と開発方針

更新日: 2026-09-06

この文書は、プロジェクトの目的と実装判断の基準をまとめたものです。記載した品質条件がすべて実装・検証済みであることは意味しません。現在の画面仕様は `docs/desktop-ui-direction.md`、更新仕様は `docs/sync-policy.md`、検証結果は各変更の記録を参照してください。

## 1. 解決する問題

**KOANとCLEに情報が散らばり、学生が本当に必要な情報へすぐ辿り着けず、課題・提出物・メッセージを見落としてしまう。**

KOAN Plusは、この問題を解決するための、ローカル完結型の学務ダッシュボードです。開けば「次に何をするか」「いつまでか」「どの授業・教室か」「何が変わったか」がわかり、必要な操作へ進めることを目指します。

主要な価値は情報の閲覧・整理・見落とし防止です。履修登録や課題提出などの正式な手続きは、KOAN/CLEの該当画面へ誘導します。公式画面全体を複製することや、取得できる情報をすべて同じ強さで並べることを目的にしません。

## 2. 対象と範囲

- 大阪大学の学生が、デスクトップのChrome / Firefox拡張機能として日常利用することを想定します。Firefox対応は1.6.0に含まれますがストア未公開で、最低対応バージョンは140です。
- 主な対象は、今日・今後の授業と変更、課題と締切・提出状態、掲示、未読メッセージ、授業資料、成績・修得単位です。
- 通常のKOAN/CLEのページデザインは変更せず、拡張機能側で統合ビューを提供します。
- 自動ログインとMFA自動化は任意の補助機能です。利用の必須条件にせず、状態・同意・停止・削除の意味を理解できるようにします。
- **モバイル専用UIやスマートフォン対応の追加は現在の対象外です。** ただし、デスクトップでも長い授業名、情報量、ウィンドウの高さなどで表示や操作が壊れる問題は修正対象です。
- 他大学向けサービスとの比較や、iOS/Androidアプリ化の相談は過去にありますが、そのまま実装予定にはしません。

## 3. 判断の基準

| 基準 | 変更を評価する問い |
| --- | --- |
| 必要な情報へすぐ辿り着く | 次の授業、近い締切、未読連絡を探す操作と時間が減るか |
| 情報を信頼できる | 未取得・失敗・一部取得・正常な0件を見分けられるか |
| 取得漏れと負荷を同時に減らす | 重複通信を減らしつつ、未取得分を後から回収できるか |
| 操作を待たせない | 保存済みの内容を使い続けられ、操作結果がすぐ伝わるか |
| 長く使い続けられる | 学期・年度の変化、データ蓄積、セッション切れから復旧できるか |
| 必要なものだけを見せる | 新しい説明、カード、件数、色に固有の役割があるか |

「低負荷のために取得漏れを黙認する」「エラーを見せないために成功扱いする」「情報を増やして重要な情報を埋める」は、この目的に反します。

## 4. デザインは見た目・動き・導線を含む

### 情報の優先順位

- 情報源の都合より、学生が次にすることを優先します。課題とアンケートなどは締切を比較できる形にし、遠い締切が今日の締切より目立つ並びを避けます。
- 期限切れの項目は消さず、これから対応できる項目より優先度を下げて整理します。期限のない課題を「不要」と判断して落とさないでください。
- 休講・教室変更は、どの授業の何が変わったかをその授業の文脈で示します。
- 授業名は日本語の名称を中心にします。コードや英訳などの補足は、必要な詳細へ寄せます。
- 時間割から授業詳細、資料、CLE・シラバスへ、視線と操作が自然につながるようにします。リンクのラベルと実際の到着先を一致させます。
- 成績は公式値を尊重します。不足した算入単位数や対象科目を推測して累積GPAを作らず、修得単位と履修した単位を混同しません。

### 静かで一貫した見た目

- 装飾的なグラデーション、意味のない色帯、大きな色面、強い影、過剰なバッジ、説明を詰めたカードを増やしません。
- 文字の階層、余白、薄い境界線を使います。関連する内容までそれぞれ別の箱へ閉じ込めないでください。
- 横並びの片側が長くなっても、もう片側を不自然に引き伸ばしません。余った場所を埋めるためだけに情報を追加しません。
- 色・文字サイズ・角丸・動きは、原則として `src/styles.css` の既存トークンを再利用します。色だけで状態を伝えず、ライト・ダーク両方の可読性を確認します。
- 既存の色の意味は、紺・淡紫系が選択や関連、橙が注意・未読・変更などを示すことを基本とします。色の追加や面積の拡大を、情報の整理の代わりにしません。
- サイドバーの選択表示や情報密度も検証対象に含めます。現在の構成を固定せず、変更が必要な場合は具体的な問題と影響を説明します。

### 説明とフィードバック

- 日常画面に「この画面で何ができるか」の宣伝的な説明を常設しません。初回画面では、使う価値を短く伝えてよい、という最新の要望があります。
- 更新状況と同じエラーを、ヘッダー・カード・サイドバーへ繰り返し表示しません。共通の要約はヘッダーに集約し、必要な詳細や再試行へ進めるようにします。
- 状況表示の出現で本文が上下に動いたり、通知が閲覧内容を覆ったりしないようにします。
- 初回取得と保存済みデータの再確認を分けます。初回は読み込み中とわかる表示、再確認中は内容を残した表示にします。
- 通信待ちやクールダウンを、理由不明の無効ボタンで表現しません。操作に反応し、保存済み結果の利用や次の再試行を伝えます。処理の二重実行防止、入力不備、同意前など、必要な無効化は理由を明確にします。
- モーダルは閉じ方、Esc、フォーカス移動・復帰を揃えます。中断すると状態が壊れる処理には、理由のある例外を設けます。
- 楽観的な既読表示などで応答を速くする場合も、公式側の状態との再照合や失敗時の整合性を考慮します。

### UIを大きく変えるとき

既存デザインにこだわらず、UX向上に必要な変更を検討できます。ただし、**画面構成や主要な導線を大きく作り変える場合は、問題・目的・具体案・影響範囲を整理して、ユーザーの同意を得てから実装します。**

既に同意された範囲の修正や通常の不具合修正について、同じ確認を繰り返す必要はありません。

## 5. 同期と取得の原則

### 保存済みデータをすぐ見せ、必要な範囲だけ再確認する

- 期限切れのキャッシュも表示に使い、更新に成功した範囲から差し替えます。保存済みであることと、現在も最新であることを混同しません。
- 変化の頻度と重要度に応じてキャッシュ期間を分けます。手動更新も、時間割・科目対応などの長期キャッシュを無条件に捨てる操作にはしません。
- 複数タブ、連打、同一URLの実行中リクエストは重複を防ぎ、他の画面が取得した結果も利用します。
- 現在は、自動ログインが有効で、画面が表示されオンラインの間に、期限を過ぎた情報を自動同期する方針です。成績と掲示も対象です。
- ローカルで同期の必要性を定期確認することと、毎回大学サーバーへ通信することを分けます。停止・非表示・オフラインの条件を守ります。

同期間隔やページ数上限の現行値は [データ更新の仕組み](./docs/sync-policy.md) と `src/sync.ts`、`src/koan.ts`、`src/cle.ts` を参照してください。本書では変動する数値を重複管理しません。

### 「取れなかった」と「存在しない」を分ける

- HTTP成功だけで取得成功とは判定しません。ログイン画面、エラーページ、想定外の構造、未完了のページ送りを検出します。
- 空の一覧で正常な保存済みデータを消さないようにします。一方、正常に確認できた0件は正しく反映します。
- 取得上限や時間制限に達したら一部取得として扱い、取得済み分と再開に必要な位置を保持します。毎回先頭へ戻って同じ範囲だけを取り続けないようにします。
- ページ送りの循環・停滞は、新しいデータを得られているかも含めて判断します。回復できる異常には限定的に対応し、無限ループを許さないようにします。
- 全体の最終更新時刻だけで、各項目の取得完了を代用しません。特に掲示ページは、軽量な新着確認と一覧全体のスナップショット同期を区別します。
- セッション切れ、通信失敗、保存失敗、未取得の残りを隠さず、必要な復旧操作を提供します。進捗のある段階取得と、進捗のない失敗も分けます。

「誰が使っても情報取得に失敗しない」は品質目標です。外部サービスの変更や障害がある以上、無条件の保証として表示せず、検証できた範囲と残る不確実性を報告します。

### 大学側への負荷と副作用を抑える

- キャッシュ、差分取得、段階取得、通信間隔、排他制御、失敗時のバックオフを組み合わせます。負荷制限は取得層でも保証します。
- 各巡回には終了条件・時間・ページ数・再試行回数の上限を設け、動き続けるスクレイパーを作りません。
- 全科目の重い詳細を毎回取得せず、期限の近い課題や現在利用している授業などを優先します。ただし、後回しにした対象を黙って取りこぼさないようにします。
- 掲示やメッセージ本文を自動で開いて既読化しません。資料など重い取得は必要な操作に結び付け、再表示時は有効なキャッシュを活用します。
- データ閲覧のための通信と、履修・提出・認証設定など状態を変える操作を区別します。CLEの情報取得はGETに限定する既存の境界を守ります。

## 6. 長期運用とローカル完結

一年以上の継続利用と、メンテナンスの少なさを設計時の評価軸にします。

- 学期・年度の切替、過年度科目、集中講義、曜日未定、複数時限、期限なし課題などを、開発者自身の履修データに存在しないという理由で除外しません。
- キャッシュ形式変更、データ量の増大、保存容量不足、部分破損、アカウント変更、MV3サービスワーカーの停止後にも、データ混同や永久待機が起きないか確認します。
- 取得、パース、保存、同期制御、表示の責務を分け、外部の構造変更への修正と回帰テストを局所化します。アプリ本体はReact・TypeScriptを基盤とし、既存のJavaScript製拡張スクリプトの全面移行は別の判断とします。
- セッション付きURLを恒久的なリンクとして保存し続けず、安定したIDと必要時の解決を使う設計を優先します。
- 開発者のバックエンドや解析サービスへ学務・認証情報を収集しません。「ローカル完結」は大学への認証・情報取得の通信まで無いという意味ではありません。
- 暗号化を過大に説明しません。同じ端末に鍵を保持する保護の限界と、MFA自動化の影響を既存のプライバシー説明と一致させます。
- 実在の学務データ、認証情報、セッションURL、実アカウントの画面・ログをOSSへ持ち込みません。テストには合成データを使用します。

これらは長期運用の確認項目であり、過去の監査で挙がった問題を現在も未修正だと断定するものではありません。対応を決める際は現行コードを再確認します。

## 7. 変更時の確認

- 何が不便・不正確だったか、変更でどう改善するかを説明できること。
- データ取得の変更では、正常な0件、一部取得、構造変化、セッション切れ、ページ送りの停止・再開、失敗時のキャッシュ保持を、変更に関係する範囲で確認すること。
- UI変更では、初回・保存済み・更新中・失敗時と、情報量が多い・少ない場合の操作を確認すること。デスクトップのライト・ダーク両方を対象とし、モバイル対応を無断で追加しないこと。
- CSSを削る場合は、文字列検索だけで未使用と判定せず、テンプレートで生成するクラス名と実表示を確認すること。過去にMFAモーダルが空になる回帰がありました。
- 自動テストの通過と、実サーバーでの動作確認を分けて報告すること。実データ検証ができなかった場合は、再現・確認手順を示すこと。

具体的なコマンドとPRの手順は [CONTRIBUTING.md](./CONTRIBUTING.md)、CLEメッセージの実環境確認は [確認手順](./docs/cle-message-verification.md) を参照してください。

## 8. 設計判断の要点

- 自動同期は、成績・掲示を含む期限切れのデータを、表示中・オンライン・自動ログイン有効の条件で取得します。
- 更新ボタンの反応と実際の通信制限を分離し、キャッシュ利用と再試行の状態を伝えます。
- 色や配置は既存トークンと情報の役割を基準に判断します。古い画面構成や提案は固定仕様ではありません。
- 初回案内には必要な説明を置き、日常の画面では重複する説明を減らします。
- 対象はデスクトップです。モバイル対応や公式操作の代行は、別途合意された範囲でのみ検討します。
- GPAは公式値を表示し、不足した単位情報から独自に通算値を推計しません。

## 9. 文書の役割

公開文書だけで開発判断を追えるようにし、個人の会話IDやローカルの履歴へのアクセスを前提にしません。

- **本書**: 目的、設計判断、品質基準。
- **AGENTS.md**: Codexなど開発エージェントが最初に確認する要点。
- **README.md**: 利用者向けの機能、現行挙動、インストール。
- **CONTRIBUTING.md**: 開発手順、テスト、PRとOSSへの持ち込み制限。
- **[デスクトップUI仕様](./docs/desktop-ui-direction.md)**: 現在の画面構成と操作。未採用の提案とは区別します。
- **PRIVACY.md / TERMS.md / SECURITY.md**: データの扱い、利用条件、脆弱性報告。
- **ローカルの `design.md`**: 7月のUI監査・改修の詳細記録。Git管理外なので、公開リポジトリでの開発に必須の資料にはしません。数値・完了印・コード名は当時の記録として読みます。

新しい要望で判断が変わったときは、該当する方針と経緯を更新します。最新の明示的なユーザー指示を優先し、過去の提案や監査結果だけを根拠に機能追加・大規模改修を始めないでください。
